# Required: an OpenRouter API key (https://openrouter.ai) so the swarm can
# generate real, task-specific deliverable content, same as Polaris's swarm does.
LLM_API_KEY=

# Optional: any OpenRouter model id. Defaults to openai/gpt-4o-mini.
LLM_MODEL=
